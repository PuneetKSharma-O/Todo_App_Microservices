apiVersion: apps/v1
kind: Deployment
metadata:
  name: add-task-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: add-task
  template:
    metadata:
      labels:
        app: add-task
    spec:
      containers:
        - name: add-task-deployment
          image: todomicroservicesacr1234.azurecr.io/add-todo-app:latest
          ports:
            - containerPort: 8000
          env:
            - name: ConnectionStrings__TodoDatabase
              value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:devtodomicrosqlsvr1234.database.windows.net,1433;Database=devtodomicrosqldb1234;Uid=sqladminuser;Pwd=P@ssw0rd1234!;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"
